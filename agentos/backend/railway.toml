[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'uvicorn agentos.backend.main:app --host 0.0.0.0 --port ${PORT}'"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
